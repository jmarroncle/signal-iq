# Copiar a ".env.local" (no se sube a git). Mismo proyecto de Supabase que usa
# la vApp de Behavioral Design ("Behavioral platform") — Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
